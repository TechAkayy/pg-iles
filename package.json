{
  "name": "pg-iles",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TechAkayy/pg-iles.git",
    "directory": "packages/vite"
  },
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open --port 5050",
    "typecheck": "vue-tsc --noEmit",
    "vue-devtools": "vue-devtools",
    "lint": "npm run lint:fix && npm run format",
    "lint:fix": "eslint --ext .js,.cjs,.ts,.vue --ignore-path .gitignore --fix src",
    "format": "prettier --write ."
  },
  "engines": {
    "node": "^14.18 || >= 16.0.0"
  },
  "devDependencies": {
    "@iconify/json": "^2.2.28",
    "@nuxt/eslint-config": "^0.1.1",
    "@pinegrow/vite-plugin": "latest",
    "@unocss/preset-icons": "^0.50.2",
    "@vue/devtools": "^6.5.0",
    "eslint": "^8.35.0",
    "eslint-config-prettier": "^8.6.0",
    "iles": "^0.8.6",
    "prettier": "^2.8.4",
    "typescript": "^4.5",
    "unocss": "^0.50.2",
    "vue-tsc": "^0.38"
  },
  "dependencies": {
    "@vueuse/core": "^9.13.0",
    "pinia": "^2.0.32"
  }
}
